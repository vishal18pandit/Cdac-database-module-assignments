
1]
	SELECT ONUM,SNUM,AMT,AMT*0.12 AS "12% COMMISSION" FROM ORDERS;

2]	
	SELECT CITY 'FOR THE CITY',max(RATING) 'RATING' FROM CUSTOMERS group by CITY;
3]	
	SELECT RATING ,CNAME,CNUM FROM CUSTOMERS ORDER BY RATING DESC;
4]	
	SELECT ODATE,sum(AMT) FROM ORDERS group by ODATE order by ODATE DESC;